#!/usr/bin/env bash
# shellcheck disable=SC1090
# shellcheck disable=SC2034
# shellcheck disable=SC2154

# defaults
_msx_module_enabled="${_language_region_enabled:-false}"
_msx_module_appcheck=()

# module main function
_msx_module_main()
{
  # Preferred languages
  defaults write NSGlobalDomain AppleLanguages -array "${_language_region_languages[@]:-"en-DE"}"

  # Region
  defaults write NSGlobalDomain AppleLocale -string "${_language_region_locale:-"en_DE"}"

  # First day of week
  # Inherit

  # Calendar
  # Inherit

  # Time format
  # Inherit

  # Temperature
  defaults write NSGlobalDomain AppleTemperatureUnit -string "${_language_region_temperature:-Celsius}"

  # Advanced
  # Measurement units
  defaults write NSGlobalDomain AppleMeasurementUnits -string "${_language_region_measurement_units:-Centimeters}"
  defaults write NSGlobalDomain AppleMetricUnits -bool "${_language_region_metric_units:-true}"
}
:
