#!/usr/bin/env bash

# Don't run directly!
[[ "$0" == "${BASH_SOURCE[0]}" ]] && exit 255

# Sample
[[ "${_sample_enabled:-false}" == false ]] && return 1

echo "[M] $(basename "${BASH_SOURCE[0]%.msx}")"

# ...
